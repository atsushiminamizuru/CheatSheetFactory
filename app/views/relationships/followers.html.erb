<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= render "users/userinfo", user: current_user %>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2><%= "#{@user.name}様のフォロワーの一覧" %></h2>
      <%= render "relationship", users: @user_followers %>
      <%= paginate @user_followers %>
    </div>

  </div>

</div>
