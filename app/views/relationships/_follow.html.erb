<% if relationship.present? %>
  <%= link_to "フォロー済", user_relationship_path(user_id: user.id, id: relationship.id),  method: :delete, class: "btn btn-danger", remote: true %>
<% else %>
  <%= link_to "フォローする", user_relationships_path(user_id: user.id), method: :post, class: "btn btn-success", remote: true %>
<% end %>
